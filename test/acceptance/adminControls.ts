export function testAdminControls(): void {
  describe("Guild Admin Controls", function () {
    it("Admin is set at guild registration time, and can be separate from domain owner");

    it("Only admin can freeze mints");

    it("Only admin can transfer minting rights");

    it("Only admin can set a new authorization policy");

    it("Only admin can set a new fee policy");
  });
}
